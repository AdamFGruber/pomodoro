import { createAction } from '@ngrx/store';

export const start = createAction('[Mode Component] Start');
export const stop = createAction('[Mode Component] Stop');
export const swap = createAction('[Mode Component] Swap');